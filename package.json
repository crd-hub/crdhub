{
  "devDependencies": {
    "vitepress": "^1.0.0-rc.22"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "deploy:qa": "vitepress build;firebase use default;firebase deploy",
    "deploy:prod": "vitepress build;firebase use prod;firebase deploy;firebase use default"
  }
}